Maison Lune — Standalone site
Open index.html in a browser, or serve the folder:
  python3 -m http.server 8000
Then visit http://localhost:8000
(Some browsers block video/scroll features when opening file:// directly — use the local server.)
